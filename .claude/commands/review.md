이 프로젝트의 변경 사항을 리뷰하라.

`review-code` 스킬을 호출해서 진행한다 — 리뷰 절차·차원·심각도 기준·출력 형식은 전부 그 스킬에 있다. 이 파일은 `/review` 라는 짧은 이름을 유지하기 위한 진입점일 뿐이며, 리뷰 규칙을 여기에 다시 적지 않는다.

인자는 그대로 넘긴다 (`/review --deep`, `/review src/services/llm`, `/review --staged` 등).
